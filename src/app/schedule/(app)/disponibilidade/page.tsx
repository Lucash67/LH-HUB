import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleDisponibilidadePage() {
  return (
    <SchedulePlaceholder
      title="Disponibilidade"
      blurb="Horário da organização, override por profissional e exceções."
    />
  );
}
