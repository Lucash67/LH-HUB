import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleAgendaPage() {
  return (
    <SchedulePlaceholder
      title="Agenda"
      blurb="Dia, semana, status e horários livres — implementação na próxima fase."
    />
  );
}
