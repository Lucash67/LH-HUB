import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleNovoAgendamentoPage() {
  return (
    <SchedulePlaceholder
      title="Novo agendamento"
      blurb="Fluxo reutilizável (cliente → serviço → profissional → horário) entra em fase posterior."
    />
  );
}
