import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleServicosPage() {
  return (
    <SchedulePlaceholder
      title="Serviços"
      blurb="Catálogo genérico (duração, preço, profissionais habilitados)."
    />
  );
}
