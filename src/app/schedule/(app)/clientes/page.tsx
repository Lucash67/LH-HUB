import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleClientesPage() {
  return (
    <SchedulePlaceholder
      title="Clientes"
      blurb="Nome, WhatsApp e histórico da organização — implementação na próxima fase."
    />
  );
}
