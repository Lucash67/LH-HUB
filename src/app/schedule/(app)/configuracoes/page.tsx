import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleConfigPage() {
  return (
    <SchedulePlaceholder
      title="Configurações"
      blurb="Dados do estabelecimento, vertical e fuso — após o onboarding."
    />
  );
}
