import { SchedulePlaceholder } from "@/components/schedule/schedule-placeholder";

export default function ScheduleEquipePage() {
  return (
    <SchedulePlaceholder
      title="Equipe"
      blurb="Profissionais da organização — conta OMNI é opcional."
    />
  );
}
